# Next-Permutation
A permutation is an arrangement of integers in a sequence. For example, [1,2,3] has permutations like [1,2,3], [1,3,2], [2,1,3], etc. The next permutation is the next lexicographically greater arrangement. If none exists, rearrange to the smallest (ascending order). Modify the array in-place using constant extra memory.
